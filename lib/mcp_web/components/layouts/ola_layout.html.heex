<!DOCTYPE html>
<html lang="en" class="[scrollbar-gutter:stable]">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <.live_title suffix=" · MCP OLA">
      <%= assigns[:page_title] || "Merchant Application" %>
    </.live_title>
    <link phx-track-static rel="stylesheet" href={~p"/assets/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/app.js"}>
    </script>
  </head>
  <body class="bg-base-200 h-screen overflow-hidden flex flex-col">
    <!-- Header -->
    <header class="bg-base-100 border-b border-base-300 h-16 flex items-center justify-between px-6 shrink-0 z-10">
      <div class="flex items-center gap-3">
        <div class="w-8 h-8 rounded-lg bg-primary flex items-center justify-center text-primary-content font-bold">
          M
        </div>
        <span class="font-bold text-lg">Merchant Portal</span>
      </div>
      <div class="flex items-center gap-4">
        <%= if @current_user do %>
          <span class="text-sm text-base-content/70"><%= @current_user.email %></span>
          <.link href={~p"/sign-out"} method="delete" class="btn btn-sm btn-ghost">Sign Out</.link>
        <% else %>
          <.link href={~p"/online-application/login"} class="btn btn-sm btn-ghost">Sign In</.link>
        <% end %>
      </div>
    </header>

    <div class="flex flex-1 overflow-hidden">
      <!-- Main Content Area (Form) -->
      <main class="flex-1 overflow-y-auto p-6 md:p-12 scroll-smooth">
        <div class="max-w-3xl mx-auto w-full">
          <.flash_group flash={@flash} />
          <%= @inner_content %>
        </div>
      </main>

      <!-- Atlas Sidebar (AI Assistant) -->
      <aside class="w-96 bg-base-100 border-l border-base-300 flex flex-col shadow-xl shrink-0 hidden lg:flex">
        <!-- Atlas Header -->
        <div class="p-4 border-b border-base-300 flex items-center gap-3 bg-base-100/50 backdrop-blur">
          <div class="avatar online">
            <div class="w-10 h-10 rounded-full bg-gradient-to-br from-primary to-secondary p-0.5">
              <div class="w-full h-full rounded-full bg-base-100 flex items-center justify-center">
                <.icon name="hero-sparkles" class="w-5 h-5 text-primary" />
              </div>
            </div>
          </div>
          <div>
            <h3 class="font-bold text-base-content">Atlas</h3>
            <p class="text-xs text-base-content/60">AI Onboarding Concierge</p>
          </div>
        </div>

        <!-- Chat Area -->
        <div class="flex-1 overflow-y-auto p-4 space-y-4" id="atlas-chat-container">
          <!-- Welcome Message -->
          <div class="chat chat-start">
            <div class="chat-image avatar">
              <div class="w-8 h-8 rounded-full bg-base-200 flex items-center justify-center">
                <.icon name="hero-sparkles" class="w-4 h-4 text-primary" />
              </div>
            </div>
            <div class="chat-bubble chat-bubble-primary text-sm">
              Hi there! I'm Atlas. I'm here to help you get your merchant account approved as quickly as possible.
            </div>
          </div>
          
          <%= if assigns[:atlas_messages] do %>
            <%= for msg <- @atlas_messages do %>
              <div class={"chat #{if msg.sender == :user, do: "chat-end", else: "chat-start"}"}>
                <div class={"chat-bubble text-sm #{if msg.sender == :user, do: "chat-bubble-neutral", else: "chat-bubble-primary"}"}>
                  <%= msg.content %>
                </div>
              </div>
            <% end %>
          <% end %>
        </div>

        <!-- Input Area -->
        <div class="p-4 border-t border-base-300 bg-base-100">
          <form phx-submit="send_chat" class="relative">
            <input 
              type="text" 
              name="message" 
              placeholder="Ask Atlas a question..." 
              class="input input-bordered w-full pr-10 text-sm"
              autocomplete="off"
            />
            <button type="submit" class="absolute right-2 top-1/2 -translate-y-1/2 btn btn-ghost btn-xs btn-circle text-primary">
              <.icon name="hero-paper-airplane" class="w-4 h-4" />
            </button>
          </form>
        </div>
      </aside>
    </div>
  </body>
</html>
