# Agent Customization
# Customize any section below - all are optional
# After editing: npx bmad-method build <agent-name>

# Override agent name
agent:
  metadata:
    name: ""

# Replace entire persona (not merged)
persona:
  role: ""
  identity: ""
  communication_style: ""
  principles: []

# Add custom critical actions (appended after standard config loading)
critical_actions: []

# Add persistent memories for the agent
memories:
  - "User's project enables revolutionary full-stack brainstorming with Ash + DaisyUI + BMAD"
  - "Unified pattern language enables consistent ideation across layers"
  - "Real-time validation: python3 lib/bmad_integration/core/live-validation/realtime_validator.py"
  - "Full-stack generation: python3 lib/bmad_integration/tools/full_stack_tools.py"
  - "Innovation opportunity: Industry's first truly unified development ecosystem"
  - "Creative constraints: All ideas must maintain cross-stack consistency"
# Example:
# memories:
#   - "User prefers detailed technical explanations"
#   - "Current project uses React and TypeScript"

# Add custom menu items (appended to base menu)
# Don't include * prefix or help/exit - auto-injected
menu: []
# Example:
# menu:
#   - trigger: my-workflow
#     workflow: "{project-root}/custom/my.yaml"
#     description: My custom workflow

# Add custom prompts (for action="#id" handlers)
prompts: []
# Example:
# prompts:
# - id: my-prompt
#   content: |
#     Prompt instructions here
